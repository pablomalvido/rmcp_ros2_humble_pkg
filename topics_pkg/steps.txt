#Create python package: 
ros2 pkg create --build-type ament_python topics_pkg

#Create python scripts for each node inside topics_pkg/topics_pkg folder

#Add entry_points for each node in setup.py

#Create launch file inside launch folder

#Add the launch folder to the data_files in setup.py

#Compile the workspace
colcon build --symlink-install