#Create cpp package: 
ros2 pkg create --build-type ament_cmake custom_msgs --dependencies rclcpp

#Create a msg folder and add the custom msgs definitions

#Create a srv folder and add the custom srvs definitions

#Edit package.xml, add:
<build_depend>rosidl_default_generators</build_depend>
<exec_depend>rosidl_default_runtime</exec_depend>
<member_of_group>rosidl_interface_packages</member_of_group>

#Edit CMakeLists.txt, add:
find_package(rosidl_default_generators REQUIRED)
rosidl_generate_interfaces(${PROJECT_NAME}
  "srv/CheckEvenOdd.srv"
  #Add here all other srvs and msgs defined in this package
  DEPENDENCIES geometry_msgs #Add also dependencies any of the above custom msgs may have 
)

#More info: https://docs.ros.org/en/humble/Tutorials/Beginner-Client-Libraries/Custom-ROS2-Interfaces.html